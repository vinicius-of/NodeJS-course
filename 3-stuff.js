const count = (arr) => {
    return 'There are ' + arr.length + ' elements in this array!'
}

const adder = (a, b) => a + b;

const pi = 3.142;

module.exports = {
    count: count,
    adder: adder,
    pi: pi
}

// console.log(counter(['shaun', 'marocs', 'crystal']));

//require (look require.js)